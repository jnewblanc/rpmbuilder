# pkginfo.sh - This file contains some static variables that are used by
# rpmcreate to generate the package

LICENSE="Fully owned by CompanyName, all rights reserved"
VENDOR_NAME="CompanyName"
VENDOR_EMAIL="info@example.com"
VENDOR_DOMAIN="example.com"

# This is only used if the spec file is located in a non standard location
#  SOURCE_FILE=<full path to the rpm spec file>
#  SOURCE_FILE=${WSROOT}/.buildenv/rpm.spec
